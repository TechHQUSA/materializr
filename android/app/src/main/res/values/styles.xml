<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. NoTitleBar but NOT Fullscreen at the theme level:
         fullscreen/immersive is applied programmatically in MaterializrActivity
         (applyWindowMode) so the app can run windowed in desktop mode. -->
    <style name="AppTheme" parent="android:Theme.NoTitleBar">
        <!-- Customize your theme here. -->
    </style>
</resources>
